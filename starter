set up github repo:
contact helpdesk to add your github acc to domain repo, two-step auth needed

set up make:
	type make in terminal, it will install

set up homebrew
	run in terminal:
		/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"

set up airflow

set up python
	brew install python
	echo "alias python='python3'" >> ~/.bash_profile
	echo "alias pip='pip3'" >> ~/.bash_profile
	
	source ~/.bash_profile
	python --version
	
set pyenv
	pyenv needs to be installed first, and use that to install python3
	